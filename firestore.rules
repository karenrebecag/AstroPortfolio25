rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /comments/{document} {
      allow read: if resource.data.status == 'approved';
      allow write: if false; // Solo via API server-side
    }
  }
}