---
export const prerender = true;

import Layout from '../layouts/Layout.astro';
import HeaderPill from '../components/ui/HeaderPill.astro';
import ParallaxBanner from '../components/ui/ParallaxBanner.astro';
import MainButton from '../components/ui/MainButton.astro';
import CodeBlock from '../components/ui/CodeBlock.astro';
import { LineHeading } from '../components/ui/LineHeading.tsx';
import { InView } from '../components/ui/InView.tsx';
import { DarkModeContainer } from '../components/ui/DarkModeContainer.tsx';
import { LucideIcon } from '../components/ui/LucideIcon.tsx';
import { AnimatedNumber } from '../components/ui/AnimatedNumber.tsx';
import { Carousel } from '../components/ui/Carousel.tsx';
import AurinMarquees from '../components/ui/AurinMarquees.astro';
import TechStack from '../components/ui/TechStack.astro';
import AurinBestFeatures from '../components/ui/AurinBestFeatures.astro';
import AurinWorkflow from '../components/ui/AurinWorkflow.astro';
import ReviewsIsland from '../components/ui/ReviewsIsland.tsx';
import ProgressiveReviewsIsland from '../components/ui/ProgressiveReviewsIsland.tsx';
import GLSLHills from '../components/ui/GLSLHills.astro';
import type { Review } from '../types/reviews';
import ShareBar from '../components/ui/ShareBar.astro';
import CommentsSection from '../components/ui/CommentsSection.astro';
import ProfileCardSection from '../components/sections/ProfileCardSection.astro';
import StickyScrollController from '../components/ui/StickyScrollController.tsx';
import MoreProjects from '../components/ui/MoreProjects.astro';
import ProjectGallerySection from '../components/ui/ProjectGallerySection.astro';
import { aurinGalleryConfig } from '../config/aurinGalleryData.ts';
import '../styles/global.css';
import '../styles/shiki-themes.css';
import '../styles/reviews-section.css';

// Code snippets for the technical documentation
const firestoreCollectionCode = `// Firebase Firestore Collection Architecture
/tasks/{taskId}
  - title, description, status, priority
  - assignees[], createdBy, timestamps
  - attachments[], comments[]

/users/{userId}
  - profile, preferences, roles
  - notifications[], settings

/notifications/{notificationId}
  - type, recipients[], status
  - metadata, timestamps

/timeEntries/{entryId}
  - taskId, userId, duration
  - timestamps, notes

/messages/{messageId}
  - conversationId, sender, content
  - type (group/private), timestamps`;

const notificationQueueCode = `class NotificationQueue {
  private queue: Map<string, QueuedNotification> = new Map();
  private processing: boolean = false;
  
  async enqueue(notification: Notification): Promise<void> {
    const queuedItem: QueuedNotification = {
      id: generateId(),
      notification,
      attempts: 0,
      maxRetries: 3,
      status: 'pending',
      createdAt: new Date(),
      nextRetry: new Date()
    };
    
    this.queue.set(queuedItem.id, queuedItem);
    await this.saveToFirestore(queuedItem);
    
    if (!this.processing) {
      this.processQueue();
    }
  }
  
  private async processQueue(): Promise<void> {
    this.processing = true;
    
    while (this.queue.size > 0) {
      const items = Array.from(this.queue.values())
        .filter(item => item.status === 'pending' && item.nextRetry <= new Date())
        .sort((a, b) => a.createdAt.getTime() - b.createdAt.getTime());
      
      for (const item of items) {
        try {
          await this.sendNotification(item.notification);
          item.status = 'sent';
          this.queue.delete(item.id);
          await this.deleteFromFirestore(item.id);
        } catch (error) {
          item.attempts++;
          
          if (item.attempts >= item.maxRetries) {
            item.status = 'failed';
            await this.logFailure(item, error);
            this.queue.delete(item.id);
          } else {
            // Exponential backoff: 2^attempts minutes
            const delayMinutes = Math.pow(2, item.attempts);
            item.nextRetry = new Date(Date.now() + delayMinutes * 60 * 1000);
            await this.updateFirestore(item);
          }
        }
      }
      
      await new Promise(resolve => setTimeout(resolve, 5000)); // Check every 5s
    }
    
    this.processing = false;
  }
}`;

const realTimeSyncCode = `class SyncEngine {
  private pendingChanges: Map<string, Change> = new Map();
  
  async updateTask(taskId: string, updates: Partial<Task>): Promise<void> {
    // 1. Immediate UI update (optimistic)
    const previousState = store.getTask(taskId);
    store.updateTask(taskId, updates);
    
    // 2. Track pending change
    const changeId = generateId();
    this.pendingChanges.set(changeId, {
      taskId,
      updates,
      previousState,
      timestamp: Date.now()
    });
    
    try {
      // 3. Send to Firestore
      await firestore.collection('tasks').doc(taskId).update({
        ...updates,
        updatedAt: serverTimestamp(),
        updatedBy: currentUser.id
      });
      
      // 4. Success - remove from pending
      this.pendingChanges.delete(changeId);
      
    } catch (error) {
      // 5. Failure - rollback UI
      store.updateTask(taskId, previousState);
      this.pendingChanges.delete(changeId);
      
      // 6. Show error to user
      toast.error('Failed to update task. Please try again.');
      
      // 7. Log for debugging
      Sentry.captureException(error, {
        extra: { taskId, updates, changeId }
      });
    }
  }
}`;

const aiIntegrationCode = `class GeminiService {
  private model = 'gemini-pro';
  private apiKey = process.env.GEMINI_API_KEY;
  
  async suggestAssignments(task: Task, team: User[]): Promise<Assignment[]> {
    const prompt = \`
      Given this task:
      Title: \${task.title}
      Description: \${task.description}
      Required Skills: \${task.skills.join(', ')}
      
      And these team members:
      \${team.map(u => \`
        - \${u.name} (\${u.role})
          Skills: \${u.skills.join(', ')}
          Current Workload: \${u.currentTasks} tasks
          Availability: \${u.availability}
      \`).join('\\n')}
      
      Suggest the best 3 team members to assign this task to, with reasoning.
      Return as JSON: [{ userId, score, reasoning }]
    \`;
    
    const response = await fetch(
      \`https://generativelanguage.googleapis.com/v1/models/\${this.model}:generateContent\`,
      {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': \`Bearer \${this.apiKey}\`
        },
        body: JSON.stringify({
          contents: [{ parts: [{ text: prompt }] }],
          generationConfig: {
            temperature: 0.3,
            topK: 40,
            topP: 0.95,
            maxOutputTokens: 1024
          }
        })
      }
    );
    
    const data = await response.json();
    const suggestions = JSON.parse(
      data.candidates[0].content.parts[0].text
    );
    
    return suggestions;
  }
}`;

const securityRulesCode = `rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    function hasRole(role) {
      return isAuthenticated() && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == role;
    }
    
    function canAccessTask(taskId) {
      let task = get(/databases/$(database)/documents/tasks/$(taskId)).data;
      return request.auth.uid in task.assignees ||
             request.auth.uid == task.createdBy ||
             hasRole('admin');
    }
    
    // Tasks collection
    match /tasks/{taskId} {
      allow read: if isAuthenticated() && canAccessTask(taskId);
      allow create: if isAuthenticated();
      allow update: if canAccessTask(taskId);
      allow delete: if hasRole('admin') || hasRole('manager');
    }
    
    // Users collection
    match /users/{userId} {
      allow read: if isAuthenticated();
      allow write: if isOwner(userId) || hasRole('admin');
    }
    
    // Notifications collection
    match /notifications/{notificationId} {
      allow read: if isAuthenticated() && 
                     resource.data.recipientId == request.auth.uid;
      allow create: if isAuthenticated();
      allow update, delete: if resource.data.recipientId == request.auth.uid;
    }
  }
}`;

const performanceMetricsCode = `// Production Performance Metrics (Average)
const coreWebVitals = {
  "Largest Contentful Paint (LCP)": "1.2s (target: <2.5s) ✅",
  "First Input Delay (FID)": "45ms (target: <100ms) ✅", 
  "Cumulative Layout Shift (CLS)": "0.05 (target: <0.1) ✅",
  "Time to First Byte (TTFB)": "280ms (target: <600ms) ✅",
  "Time to Interactive (TTI)": "1.8s (target: <3.5s) ✅"
};

const lighthouseScores = {
  "Performance": "98/100",
  "Accessibility": "100/100", 
  "Best Practices": "100/100",
  "SEO": "100/100"
};

// API Performance (95th percentile)
const apiResponseTimes = {
  "GET /api/tasks": "120ms",
  "POST /api/tasks": "180ms", 
  "PUT /api/tasks/[id]": "150ms",
  "GET /api/notifications": "90ms",
  "POST /api/ai/suggest": "2.1s (AI processing)"
};

// Load Test Results
const loadTestResults = {
  "Concurrent Users": 1000,
  "Duration": "10 minutes",
  "Total Requests": 250000,
  "Success Rate": "99.98%",
  "Average Response Time": "145ms",
  "95th Percentile": "320ms",
  "Throughput": "416 req/sec",
  "Error Rate": "0.02%"
}`;

// Aurin Task Manager specific reviews data
const aurinReviews: Review[] = [
  {
    id: "aurin-1",
    name: "Carlos Mendoza",
    position: "CTO at Aurin",
    review: "Karen delivered an exceptional task management platform that transformed our team's productivity. The real-time collaboration features and AI integration exceeded our expectations completely.",
    rating: 5,
    timestamp: new Date('2024-08-15'),
    status: 'approved'
  },
  {
    id: "aurin-2", 
    name: "Sofia Rodriguez",
    position: "Project Manager at Aurin",
    review: "The notification system and workflow automation Karen built saved us 15+ hours per week. The attention to detail in UX design makes complex project management feel effortless.",
    rating: 5,
    timestamp: new Date('2024-08-20'),
    status: 'approved'
  },
  {
    id: "aurin-3",
    name: "Miguel Santos",
    position: "Lead Developer at Aurin", 
    review: "Working with Karen was incredible. Her technical expertise in Next.js, TypeScript, and Firebase resulted in a robust, scalable platform that handles our enterprise needs perfectly.",
    rating: 5,
    timestamp: new Date('2024-09-01'),
    status: 'approved'
  },
  {
    id: "aurin-4",
    name: "Ana Gutierrez",
    position: "Operations Director at Aurin",
    review: "The cost reduction from $15/user to $0.29/user while improving functionality is remarkable. Karen's solution delivered 16,580% ROI in the first year. Outstanding work!",
    rating: 5,
    timestamp: new Date('2024-09-10'),
    status: 'approved'
  }
];
---

<Layout title="Karen Ortiz - Aurin Task Manager" lang="en">
	<!-- Critical Preconnects - Must be first -->
	<link slot="head" rel="preconnect" href="https://pub-3ed7c563bcaa4c7c8ed703c87bbc1631.r2.dev" crossorigin>
	<link slot="head" rel="preconnect" href="https://pub-2e7dc04d482146c59f472ab28fba09a9.r2.dev" crossorigin>
	<link slot="head" rel="preconnect" href="https://hebbkx1anhila5yf.public.blob.vercel-storage.com">
	<link slot="head" rel="preconnect" href="https://fonts.googleapis.com">
	<link slot="head" rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	
	<!-- Critical Resource Preloads for this page -->
	<link slot="head" rel="preload" href="https://hebbkx1anhila5yf.public.blob.vercel-storage.com/alt-g7Cv2QzqL3k6ey3igjNYkM32d8Fld7.mp4" as="video" type="video/mp4" fetchpriority="high">
	<link slot="head" rel="preload" href="https://pub-3ed7c563bcaa4c7c8ed703c87bbc1631.r2.dev/CleanShot%202025-09-25%20at%2013.56.30%402x.webp" as="image" type="image/webp" fetchpriority="high">
	
	<!-- Cache optimization meta tags -->
	<meta slot="head" http-equiv="Cache-Control" content="public, max-age=31536000, immutable">
	<meta slot="head" name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
	
	<!-- Critical CSS for this page - Inline for faster LCP -->
	<style slot="head">
		/* Critical page-specific styles */
		.parallax-banner-section{min-height:100vh;position:relative;overflow:hidden;display:flex;align-items:center;justify-content:center}
		.executive-summary-image{min-height:400px;display:flex;align-items:center;justify-content:center}
		.executive-summary-image img{width:100%;max-width:1200px;height:auto;object-fit:contain}
		.content-section{padding:4rem 0;contain:layout style}
		.section-container{max-width:1200px;margin:0 auto;padding:0 2rem}
		/* Prevent layout shifts */
		.info-card{min-height:80px;contain:layout style}
		.feature-card{min-height:200px;contain:layout style}
		/* GPU acceleration for performance */
		.parallax-banner-section,.executive-summary-image,.content-section{will-change:transform;transform:translateZ(0)}
		/* Lazy loading optimization */
		.lazy-section{opacity:0;transform:translateY(20px);transition:opacity 0.6s ease,transform 0.6s ease}
		.lazy-section.loaded{opacity:1;transform:translateY(0)}
	</style>
	
	<!-- Defer non-critical JavaScript -->
	<script slot="head" type="module">
		// Lazy load non-critical components
		const observer = new IntersectionObserver((entries) => {
			entries.forEach(entry => {
				if (entry.isIntersecting) {
					entry.target.classList.add('loaded');
					observer.unobserve(entry.target);
				}
			});
		}, { rootMargin: '50px' });
		
		document.addEventListener('DOMContentLoaded', () => {
			document.querySelectorAll('.lazy-section').forEach(el => observer.observe(el));
		});
	</script>
	
	<HeaderPill />

	<!-- Parallax Banner Strip -->
	<ParallaxBanner
		title="Aurin Task Manager"
		subtitle="Enterprise-Grade Task Management with AI Integration - A Comprehensive Success Story"
		backgroundSrc="https://hebbkx1anhila5yf.public.blob.vercel-storage.com/alt-g7Cv2QzqL3k6ey3igjNYkM32d8Fld7.mp4"
		placeholder=""
		height="100vh"
	>
		<div slot="actions" class="banner-actions">
			<MainButton href="#project-summary" />
		</div>
	</ParallaxBanner>

	<DarkModeContainer client:load>
		<!-- Main Content Sections -->
		<main class="main-content" id="main-content">
		<!-- Overview Section -->
		<section class="content-section" id="overview">
			<div class="section-container">

				<div class="executive-summary-image">
					<img
						src="https://pub-3ed7c563bcaa4c7c8ed703c87bbc1631.r2.dev/Frame%202147225832.webp"
						alt="Aurin Task Manager Showcase"
						loading="eager"
						decoding="async"
						fetchpriority="high"
						width="1200"
						height="269"
					/>
				</div>

				<div class="section-header" style="padding-top: 2rem;">
					<div class="section-title" id="project-summary">
						<LineHeading client:visible>Project Summary</LineHeading>
					</div>
					<div class="section-label">Enterprise Success Story</div>
				</div>

				<InView client:idle
					variants={{
						hidden: { opacity: 0, y: 30, filter: "blur(2px)" },
						visible: { opacity: 1, y: 0, filter: "blur(0px)" },
					}}
					viewOptions={{ margin: "0px 0px -100px 0px" }}
					transition={{ duration: 0.6, ease: [0.25, 0.46, 0.45, 0.94] }}
				>
					<div class="content-grid">
						<div class="content-main">
							<p class="content-description">
								Aurin Task Manager represents a paradigm shift in collaborative task management, combining cutting-edge web technologies with artificial intelligence to deliver an enterprise-ready solution. Developed by Karen Rebeca Ortiz over six months of intensive development, this platform addresses the critical challenges of modern team coordination: fragmented communication, lack of transparency, and inefficient workflow management.
							</p>
							
							<div class="content-highlights">
								<div class="highlight-item">
									<div class="highlight-number">01</div>
									<div class="highlight-content">
										<h3>Dual AI Integration</h3>
										<p>Google Gemini and OpenAI GPT integration for intelligent task suggestions, bottleneck detection, and automated content generation.</p>
									</div>
								</div>
								
								<div class="highlight-item">
									<div class="highlight-number">02</div>
									<div class="highlight-content">
										<h3>Real-time Sync Engine</h3>
										<p>Sub-100ms update propagation with optimistic updates, conflict resolution, and Firebase real-time synchronization across all connected clients.</p>
									</div>
								</div>
								
								<div class="highlight-item">
									<div class="highlight-number">03</div>
									<div class="highlight-content">
										<h3>Advanced Notification Queue</h3>
										<p>Intelligent multi-channel notification system with email templates, retry logic, and smart filtering to keep teams informed without overwhelm.</p>
									</div>
								</div>
							</div>
						</div>
						
						<div class="content-sidebar">
							<div class="info-card">
								<div class="info-label">Project Type</div>
								<div class="info-value">Enterprise SaaS Platform</div>
							</div>
							
							<div class="info-card">
								<div class="info-label">Development Time</div>
								<div class="info-value">6 Months</div>
							</div>
							
							<div class="info-card">
								<div class="info-label">Client</div>
								<div class="info-value">Aurin</div>
							</div>
							
							<div class="info-card">
								<div class="info-label">Status</div>
								<div class="info-value status-active">
									<div class="status-dot"></div>
									Live Production
								</div>
							</div>
						</div>
					</div>
				</InView>

		
			</div>
		</section>

		<!-- The Spark Section -->
		<section class="content-section" id="the-spark">
			<div class="section-container">
				<div class="section-header">
					<div class="section-title">
						<LineHeading client:visible>The Spark</LineHeading>
					</div>
					<div class="section-label">Origin Story</div>
				</div>

				<InView client:visible
					variants={{
						hidden: { opacity: 0, y: 30, filter: "blur(2px)" },
						visible: { opacity: 1, y: 0, filter: "blur(0px)" },
					}}
					viewOptions={{ margin: "0px 0px -100px 0px" }}
					transition={{ duration: 0.6, ease: [0.25, 0.46, 0.45, 0.94], delay: 0.1 }}
				>
					<div class="story-content text-center">
						<p class="text-center ">Wrike changed how I see project management. But when I tried to build my own task manager, not everything went as planned in the tutorials... Aurin Task Manager was born from an obsession: replicating Wrike's brutal efficiency, but with my own signature. An enterprise task manager with real-time notifications, integrated encrypted chat, per-task time tracking, and instant collaboration with Firestore. Sounds impressive, right? Well, let me tell you the truth: the code wasn't always pretty. ChatSidebar.tsx has 2,066 lines. Yes, you read that right. It works, but it was my greatest lesson in humility. I learned more building (and breaking) Aurin than in years of tutorials. This article is my treasure map for you: what technologies to use, how to modularize correctly, mistakes that cost me entire nights, and updated resources so you don't stumble the same way. If you're thinking about building your own SaaS, buckle up: here come the brutal lessons.</p>
					</div>
				</InView>
			</div>
		</section>


		<!-- Project Gallery Section - Modularized -->
		<ProjectGallerySection 
			title={aurinGalleryConfig.title}
			label={aurinGalleryConfig.label}
			slides={aurinGalleryConfig.slides}
		/>

		<!-- The Great Lesson Section -->
		<section class="content-section" id="modularization-lesson">
			<div class="section-container">
				<div class="section-header">
					<div class="section-title">
						<LineHeading client:visible>The Greatest Lesson: Modularization</LineHeading>
					</div>
					<div class="section-label">Hard-Learned Wisdom</div>
				</div>

				<InView client:visible
					variants={{
						hidden: { opacity: 0, y: 30, filter: "blur(2px)" },
						visible: { opacity: 1, y: 0, filter: "blur(0px)" },
					}}
					viewOptions={{ margin: "0px 0px -100px 0px" }}
					transition={{ duration: 0.6, ease: [0.25, 0.46, 0.45, 0.94], delay: 0.1 }}
				>
					<div class="lesson-content">
						<div class="achievements-list">
							<div class="info-card flex-row flex">
								<div class="achievement-number">1</div>
								<div class="achievement-text">Complete chat system with encrypted messages using useEncryption hook</div>
							</div>
							<div class="info-card flex-row flex">
								<div class="achievement-number">2</div>
								<div class="achievement-text">Drag-to-reply interactions with fluid micro-animations (Framer Motion + custom useMessageDrag hook)</div>
							</div>
							<div class="info-card flex-row flex">
								<div class="achievement-number">3</div>
								<div class="achievement-text">Optimistic updates thanks to Zustand: UI responds instantly, Firestore syncs afterward</div>
							</div>
							<div class="info-card flex-row flex">
								<div class="achievement-number">4</div>
								<div class="achievement-text">Real-time synchronization with Firestore listeners (onSnapshot)</div>
							</div>
							<div class="info-card flex-row flex">
								<div class="achievement-number">5</div>
								<div class="achievement-text">Integrated timer per task with custom hooks (useTimerStoreHook)</div>
							</div>
							<div class="info-card flex-row flex">
								<div class="achievement-number">6</div>
								<div class="achievement-text">Infinite pagination with useMessagePagination hook</div>
							</div>
							<div class="info-card flex-row flex">
								<div class="achievement-number">7</div>
								<div class="achievement-text">Enterprise-grade notifications with notificationService</div>
							</div>
						</div>

						<div class="info-card">
							<h3 class="lesson-title">The biggest lesson I take away:</h3>
							<p>I've learned that modularization isn't about showing off architecture, it's for your future sanity. If a component exceeds 300-400 lines, <strong>it's time to stop and refactor.</strong></p>
						
							<div >
								<h3 class="resources-title">Key Resources:</h3>
								<ul class="resources-list">
									<li><a href="#" class="resource-link">Atomic Design Methodology</a></li>
									<li><a href="#" class="resource-link">React Patterns 2025</a></li>
									<li><a href="#" class="resource-link">Component Composition Best Practices</a></li>
								</ul>
							</div>
						</div>


					</div>
				</InView>
			</div>
		</section>

		<!-- Features Section -->
		<section class="content-section" id="features">
			<div class="section-container">
				<div class="section-header">
					<div class="section-title">
						<LineHeading client:visible>Core Features</LineHeading>
					</div>
					<div class="section-label">Technical Excellence</div>
				</div>

				<InView client:visible
					variants={{
						hidden: { opacity: 0, y: 30, filter: "blur(2px)" },
						visible: { opacity: 1, y: 0, filter: "blur(0px)" },
					}}
					viewOptions={{ margin: "0px 0px -100px 0px" }}
					transition={{ duration: 0.6, ease: [0.25, 0.46, 0.45, 0.94], delay: 0.1 }}
				>
					<div class="features-grid">
						<div class="info-card flex flex-col items-center text-center">
							<div class="feature-icon">
								<LucideIcon client:load name="zap" size={40} />
							</div>
							<h4>100% TypeScript Architecture</h4>
							<p>Complete type safety with Next.js 15.3.3, React 19, and strict TypeScript configuration ensuring robust, maintainable code.</p>
						</div>
						
						<div class="info-card flex flex-col items-center text-center">
							<div class="feature-icon">
								<LucideIcon client:load name="flame" size={40} />
							</div>
							<h4>Firebase Real-time Database</h4>
							<p>NoSQL Firestore with real-time synchronization, offline support, and granular security rules for scalable data management.</p>
						</div>
						
						<div class="info-card flex flex-col items-center text-center">
							<div class="feature-icon">
								<LucideIcon client:load name="shield" size={40} />
							</div>
							<h4>Dual Authentication System</h4>
							<p>Clerk for user management with Firebase Auth for backend security, supporting social logins, MFA, and role-based access control.</p>
						</div>
						
						<div class="info-card flex flex-col items-center text-center">
							<div class="feature-icon">
								<LucideIcon client:load name="bot" size={40} />
							</div>
							<h4>Advanced Email System</h4>
							<p>Professional email templates with Nodemailer, smart notification queuing, retry logic, and batch processing capabilities.</p>
						</div>
						
						<div class="info-card flex flex-col items-center text-center">
							<div class="feature-icon">
								<LucideIcon client:load name="trending-up" size={40} />
							</div>
							<h4>Production Performance</h4>
							<p>98+ Lighthouse score, sub-second load times, 99.98% uptime, and Core Web Vitals optimization on Vercel edge network.</p>
						</div>
						
						<div class="info-card flex flex-col items-center text-center">
							<div class="feature-icon">
								<LucideIcon client:load name="dollar-sign" size={40} />
							</div>
							<h4>Cost-Effective Scaling</h4>
							<p>$0.29 per user monthly cost with 96% cost reduction vs traditional solutions and 16,580% annual ROI.</p>
						</div>
					</div>
				</InView>
			</div>
		</section>

		<!-- Aurin Marquees Section -->
		<AurinMarquees />

		<!-- Tech Stack Section -->
		<TechStack />

		<!-- Aurin Best Features Section -->
		<AurinBestFeatures />

		<!-- Aurin Workflow Section -->
		<AurinWorkflow />

		<!-- Client Reviews Section -->
		<div class="sticky-scroll-wrapper">
			<section class="reviews-section sticky-reviews">
				<div class="reviews-section__container">
					<div class="reviews-section__reviews">
					<!-- GLSL Hills Background -->
					<div class="reviews-glsl-background">
						<GLSLHills 
							height="100%" 
							cameraZ={125}
							planeSize={256}
							speed={0.3}
							className="reviews-hills-bg"
						/>
					</div>
					<ProgressiveReviewsIsland 
						reviews={[
							{
								id: "aurin-1",
								name: "Carlos Mendoza",
								position: "CTO at Aurin",
								review: "Karen delivered an exceptional task management platform that transformed our team's productivity. The real-time collaboration features and AI integration exceeded our expectations completely.",
								rating: 5,
								timestamp: new Date('2024-08-15'),
								status: 'approved'
							},
							{
								id: "aurin-2", 
								name: "Sofia Rodriguez",
								position: "Project Manager at Aurin",
								review: "The notification system and workflow automation Karen built saved us 15+ hours per week. The attention to detail in UX design makes complex project management feel effortless.",
								rating: 5,
								timestamp: new Date('2024-08-20'),
								status: 'approved'
							},
							{
								id: "aurin-3",
								name: "Miguel Santos",
								position: "Lead Developer at Aurin", 
								review: "Working with Karen was incredible. Her technical expertise in Next.js, TypeScript, and Firebase resulted in a robust, scalable platform that handles our enterprise needs perfectly.",
								rating: 5,
								timestamp: new Date('2024-09-01'),
								status: 'approved'
							},
							{
								id: "aurin-4",
								name: "Ana Gutierrez",
								position: "Operations Director at Aurin",
								review: "The cost reduction from $15/user to $0.29/user while improving functionality is remarkable. Karen's solution delivered 16,580% ROI in the first year. Outstanding work!",
								rating: 5,
								timestamp: new Date('2024-09-10'),
								status: 'approved'
							}
						]}
						layout="grid"
						theme="light"
						showSubmitButton={true}
						client:visible 
					/>
					</div>
				</div>
			</section>
		</div>


		<!-- More Projects Section -->
		<MoreProjects />
		


		<!-- Live Application Section -->
		<section class="content-section" id="live-app">
			<div class="section-container">
				<div class="section-header">
					<div class="section-title">
						<LineHeading client:visible>Live Application</LineHeading>
					</div>
					<div class="section-label">Production Ready</div>
				</div>

				<InView client:visible
					variants={{
						hidden: { opacity: 0, y: 30, filter: "blur(2px)" },
						visible: { opacity: 1, y: 0, filter: "blur(0px)" },
					}}
					viewOptions={{ margin: "0px 0px -100px 0px" }}
					transition={{ duration: 0.6, ease: [0.25, 0.46, 0.45, 0.94] }}
				>
					<div class="live-app-content">
						<div class="app-info">
							<div class="app-details">
								<h3>Production Deployment</h3>
								<p class="app-description">
									Aurin Task Manager is live and serving 500+ active users with enterprise-grade reliability.
									The application demonstrates real-world scalability and performance in a production environment.
								</p>

								<div class="app-links">
									<div class="link-item">
										<div class="link-label">
											<LucideIcon client:load name="external-link" size={16} />
											Live Application
										</div>
										<a href="https://pm.aurincloud.com/sign-in" target="_blank" rel="noopener noreferrer" class="link-url">
											pm.aurincloud.com
										</a>
									</div>

									<div class="link-item">
										<div class="link-label">
											<LucideIcon client:load name="github" size={16} />
											Source Code
										</div>
										<a href="https://github.com/KarenRebecaOrtiz/Aurin-Task-Manager" target="_blank" rel="noopener noreferrer" class="link-url">
											github.com/KarenRebecaOrtiz/Aurin-Task-Manager
										</a>
									</div>

								

									<div class="link-item">
										<div class="link-label">
											<LucideIcon client:load name="mail" size={16} />
											Client Contact
										</div>
										<a href="mailto:sodioinfo@gmail.com" class="link-url">
											sodioinfo@gmail.com
										</a>
									</div>
								</div>
							</div>

							<div class="app-stats">
								<div class="stat-item">
									<div class="stat-icon">
										<LucideIcon client:load name="calendar" size={20} />
									</div>
									<div class="stat-value">
										<AnimatedNumber client:load value={6} />
									</div>
									<div class="stat-label">Months Development</div>
								</div>

								<div class="stat-item">
									<div class="stat-icon">
										<LucideIcon client:load name="dollar" size={20} />
									</div>
									<div class="stat-value">
										<AnimatedNumber client:load value={0.29} prefix="$" format={{ maximumFractionDigits: 2 }} />
									</div>
									<div class="stat-label">Cost per User/Month</div>
								</div>

								<div class="stat-item">
									<div class="stat-icon">
										<LucideIcon client:load name="percent" size={20} />
									</div>
									<div class="stat-value">
										<AnimatedNumber client:load value={100} suffix="%" />
									</div>
									<div class="stat-label">TypeScript Coverage</div>
								</div>

								<div class="stat-item">
									<div class="stat-icon">
										<LucideIcon client:load name="clock" size={20} />
									</div>
									<div class="stat-value">24/7</div>
									<div class="stat-label">Production Uptime</div>
								</div>
							</div>
						</div>
					</div>
				</InView>

			
			</div>
		</section>

		<ProfileCardSection />


		<!-- Share Bar Section -->
		<ShareBar />

		<!-- Comments Section -->
		<CommentsSection storyId="aurin-task-manager" />


		</main>
	</DarkModeContainer>
</Layout>

<style>
	/* Banner Actions */
	.banner-actions {
		display: flex;
		gap: 1rem;
		justify-content: center;
		align-items: center;
		flex-wrap: wrap;
	}

	/* Main Content Layout */
	.main-content {
		background: hsl(0, 0%, 99%);
		width: 100%;
		padding: 0;
	}

	.content-section {
		padding: 6rem 2rem;
		border-bottom: 1px solid #f0f0f0;
		background-image:
			linear-gradient(rgba(0, 0, 0, 0.020) 1px, transparent 1px),
			linear-gradient(90deg, rgba(0, 0, 0, 0.020) 1px, transparent 1px);
		background-size: 20px 20px;
		position: relative;
		transition: background-image 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94),
		            border-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	/* Dark Mode - Content Section */
	:global(.dark-mode .content-section) {
		border-bottom-color: rgba(176, 196, 230, 0.1);
		background-image:
			linear-gradient(rgba(176, 196, 230, 0.05) 1px, transparent 1px),
			linear-gradient(90deg, rgba(176, 196, 230, 0.05) 1px, transparent 1px);
	}

	.content-section:last-child {
		border-bottom: none;
	}

	.section-container {
		max-width: 1200px;
		margin: 0 auto;
		width: 100%;
	}

	/* Section Headers - Following resume.astro structure */
	.section-header {
		margin: 8rem 0rem 6rem 0rem;
		text-align: center;
	}

	.section-title {
		font-family: var(--font-display, 'Median', serif);
		font-size: clamp(2.5rem, 6vw, 4rem);
		font-weight: 400;
		line-height: 0.9;
		margin-bottom: 1rem;
		color: #1B1B1B;
	}

	.section-label {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 0.875rem;
		font-weight: 500;
		color: #666;
		text-transform: uppercase;
		letter-spacing: 0.1em;
	}

	/* Content Grid Layout */
	.content-grid {
		display: grid;
		grid-template-columns: 2fr 1fr;
		gap: 4rem;
		align-items: start;
	}

	.content-main {
		display: flex;
		flex-direction: column;
		gap: 3rem;
	}

	.content-description {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		line-height: 1.6;
		color: #333;
		margin: 0;
	}

	/* Content Highlights */
	.content-highlights {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	.highlight-item {
		display: flex;
		gap: 1.5rem;
		align-items: flex-start;
	}

	.highlight-number {
		font-family: var(--font-display, 'Median', serif);
		font-size: 1.5rem;
		font-weight: 400;
		color: #4523AE;
		min-width: 3rem;
		text-align: center;
	}

	.highlight-content h4 {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 0.5rem 0;
	}

	.highlight-content p {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.5;
		color: #666;
		margin: 0;
	}

	/* Sidebar Info Cards */
	.content-sidebar {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.info-card {
		padding: 1.5rem;
		backdrop-filter: blur(10px) brightness(0.8);
		border-radius: 12px;
		border: 1px solid transparent;
		background-image: linear-gradient(#ffffff, rgb(250, 250, 250)), linear-gradient(to bottom, rgba(255, 255, 255, 1), rgba(255, 255, 255, 0));
		background-origin: border-box;
		background-clip: padding-box, border-box;
	}

	.info-label {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 0.875rem;
		font-weight: 500;
		color: #666;
		margin-bottom: 0.5rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	.info-value {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		font-weight: 600;
		color: #1B1B1B;
	}

	.status-active {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		color: #22c55e;
	}

	.status-dot {
		width: 8px;
		height: 8px;
		background: #22c55e;
		border-radius: 50%;
		animation: pulse 2s infinite;
	}

	@keyframes pulse {
		0%, 100% { opacity: 1; }
		50% { opacity: 0.5; }
	}

	/* Features Grid */
	.features-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
		gap: 2rem;
	}

	.feature-card {
		padding: 2rem;
		background: white;
		border: 1px solid #e9ecef;
		border-radius: 16px;
		text-align: center;
		transition: all 0.3s ease;
	}

	.feature-card:hover {
		transform: translateY(-4px);
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
		border-color: #4523AE;
	}

	.feature-icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
		display: flex;
		justify-content: center;
		align-items: center;
		color: #4523AE;
	}

	.feature-card h3 {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.25rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 1rem 0;
	}

	.feature-card p {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.5;
		color: #666;
		margin: 0;
	}

	/* Technology Stack */
	.tech-categories {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 3rem;
	}

	.tech-category h4 {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 1.5rem 0;
	}

	.tech-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
	}

	.tech-tag {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 0.875rem;
		font-weight: 500;
		padding: 0.5rem 1rem;
		background: #f8f9fa;
		color: #4523AE;
		border: 1px solid #e9ecef;
		border-radius: 20px;
		transition: all 0.2s ease;
	}

	.tech-tag:hover {
		background: #4523AE;
		color: white;
		transform: translateY(-1px);
	}

	/* Responsive Design */
	@media (max-width: 768px) {
		.banner-actions {
			flex-direction: column;
			gap: 0.75rem;
		}

		.content-section {
			padding: 4rem 1rem;
		}

		.content-grid {
			grid-template-columns: 1fr;
			gap: 3rem;
		}

		.section-header {
			margin-bottom: 3rem;
		}

		.features-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.feature-card {
			padding: 1.5rem;
		}

		.tech-categories {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.highlight-item {
			gap: 1rem;
		}

		.highlight-number {
			min-width: 2rem;
			font-size: 1.25rem;
		}
	}

	@media (max-width: 480px) {
		.content-section {
			padding: 3rem 1rem;
		}

		.section-title {
			font-size: clamp(2rem, 8vw, 3rem);
		}
	}

	/* Lesson Content Styles */
	.lesson-content {
		display: flex;
		flex-direction: column;
		gap: 3rem;
	}

	.lesson-intro {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.achievements-list {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.achievement-item {
		display: flex;
		flex-direction: row;
		gap: 1rem;
		align-items: flex-start;
	}

	.achievement-number {
		font-family: var(--font-display, 'Median', serif);
		font-size: 1.5rem;
		font-weight: 400;
		min-width: 2rem;
		color: #4523AE;
	}

	.achievement-text {
		flex: 1;
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.5;
		color: #333;
	}

	.mistakes-section {
		margin-top: 2rem;
	}

	.mistakes-title {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.25rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 1rem 0;
	}

	.mistake-highlight {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		font-weight: 700;
		color: #dc2626;
		margin: 0 0 1rem 0;
	}

	.mistakes-list {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.5;
		color: #666;
		margin: 0;
		padding-left: 1.5rem;
	}

	.mistakes-list li {
		margin-bottom: 0.5rem;
	}

	.architecture-section {
		margin-top: 2rem;
	}

	.architecture-title {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.25rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 1.5rem 0;
	}

	.key-lesson {
		margin-top: 2rem;
		padding: 2rem;
		background: white;
		border-radius: 16px;
		box-shadow: 0 4px 16px rgba(0, 0, 0, 0.05);
	}

	.lesson-title {
		font-family: var(--font-display, 'Median', serif);
		font-size: 1.25rem;
		font-weight: 400;
		color: #1B1B1B;
		margin: 0 0 1rem 0;
	}

	.key-lesson p {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.6;
		color: #333;
		margin: 0;
	}

	.resources-section {
		margin-top: 2rem;
	}

	.resources-title {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 1rem 0;
	}

	.resources-list {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.5;
		margin: 0;
		padding-left: 1.5rem;
	}

	.resources-list li {
		margin-bottom: 0.5rem;
	}

	.resource-link {
		color: #4523AE;
		text-decoration: underline;
		transition: color 0.2s ease;
	}

	.resource-link:hover {
		color: #6d28d9;
	}

	/* Text utility classes for lesson content */
	.text-normal {
		font-weight: 400;
	}

	.text-bold {
		font-weight: 700;
	}

	/* Dark Mode Styles */
	:global(.dark-mode) {
		color-scheme: dark;
	}

	/* HeaderPill Dark Mode Styles */
	:global(.dark-mode .header-pill) {
		background: rgba(17, 17, 17, 0.95);
		border-color: rgba(255, 255, 255, 0.1);
		box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
	}

	:global(.dark-mode .header-pill.scrolled) {
		background: rgba(17, 17, 17, 0.98);
		box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4);
		border-color: rgba(255, 255, 255, 0.15);
	}

	:global(.dark-mode .logo) {
		color: #ffffff;
	}

	:global(.dark-mode .logo:hover) {
		color: #e5e5e5;
	}

	:global(.dark-mode .nav a) {
		color: #e5e5e5;
	}

	:global(.dark-mode .nav a:hover) {
		color: #ffffff;
	}

	:global(.dark-mode .nav a::after) {
		background: #ffffff;
	}

	:global(.dark-mode .menu-btn span) {
		background: #ffffff;
	}

	:global(.dark-mode .menu-btn:hover span) {
		background: #e5e5e5;
	}

	/* Mobile Menu Dark Mode - Already dark, but ensure consistency */
	:global(.dark-mode .mobile-menu) {
		background: rgba(0, 0, 0, 0.98);
	}

	:global(.dark-mode .main-content) {
		background: linear-gradient(to bottom, #050112 0%, #0a0820 100%);
		color: #f0f4ff;
	}

	:global(.dark-mode .content-section) {
		background: #050112;
		border-bottom-color: rgba(176, 196, 230, 0.1);
		background-image:
			linear-gradient(rgba(176, 196, 230, 0.05) 1px, transparent 1px),
			linear-gradient(90deg, rgba(176, 196, 230, 0.05) 1px, transparent 1px);
	}

	:global(.dark-mode .section-title) {
		color: #f0f4ff;
	}

	:global(.dark-mode .section-label) {
		color: #b8c4e6;
	}

	:global(.dark-mode .content-description) {
		color: #e5e5e5;
	}

	:global(.dark-mode .highlight-content h4) {
		color: #ffffff;
	}

	:global(.dark-mode .highlight-content p) {
		color: #cccccc;
	}

	:global(.dark-mode .info-card) {
		background: #1f1f1f;
		border-color: #333333;
	}

	:global(.dark-mode .info-value) {
		color: #ffffff;
	}

	:global(.dark-mode .info-label) {
		color: #cccccc;
	}

	:global(.dark-mode .feature-card) {
		background: #1f1f1f;
		border-color: #333333;
		color: #ffffff;
	}

	:global(.dark-mode .feature-card:hover) {
		border-color: #4523AE;
		background: #252525;
	}

	:global(.dark-mode .feature-card h3) {
		color: #ffffff;
	}

	:global(.dark-mode .feature-card p) {
		color: #cccccc;
	}

	:global(.dark-mode .feature-icon) {
		color: #4523AE;
	}

	:global(.dark-mode .tech-category h4) {
		color: #ffffff;
	}

	:global(.dark-mode .tech-tag) {
		background: #1f1f1f;
		border-color: #333333;
		color: #4523AE;
	}

	:global(.dark-mode .tech-tag:hover) {
		background: #4523AE;
		color: #ffffff;
	}

	/* Smooth transitions for theme switching - Coordinated with Motion.dev */
	:global(.main-content),
	:global(.content-section),
	:global(.section-title),
	:global(.section-label),
	:global(.content-description),
	:global(.highlight-content h4),
	:global(.highlight-content p),
	:global(.info-card),
	:global(.info-value),
	:global(.info-label),
	:global(.feature-card),
	:global(.feature-card h3),
	:global(.feature-card p),
	:global(.tech-category h4),
	:global(.tech-tag),
	:global(.header-pill),
	:global(.logo),
	:global(.nav a),
	:global(.nav a::after),
	:global(.menu-btn span),
	:global(.mobile-menu) {
		transition: 
			background-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), 
			color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), 
			border-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94),
			box-shadow 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	/* Enhanced smooth transitions for specific elements */
	:global(.feature-card) {
		transition: 
			background-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), 
			color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), 
			border-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94),
			box-shadow 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94),
			transform 0.3s ease;
	}

	:global(.tech-tag) {
		transition: 
			background-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), 
			color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), 
			border-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94),
			transform 0.2s ease;
	}

	/* Smooth body and document transitions */
	:global(body) {
		transition: background-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	:global(html) {
		transition: background-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	/* Profile Card Section Styles */
	.profile-card-section {
		padding: 4rem 2rem;
		background: transparent;
		display: flex;
		justify-content: center;
		align-items: center;
		border-bottom: 1px solid #f0f0f0;
	}

	.profile-card-wrapper {
		width: 100%;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	/* Dark mode styles for profile card section */
	:global(.dark-mode .profile-card-section) {
		border-bottom-color: #333333;
	}

	/* Responsive adjustments */
	@media (max-width: 768px) {
		.profile-card-section {
			padding: 3rem 1rem;
		}
	}

	/* Stagger animation delays for content sections */
	:global(.content-section:nth-child(1)) {
		transition-delay: 0.05s;
	}

	:global(.content-section:nth-child(2)) {
		transition-delay: 0.1s;
	}

	:global(.content-section:nth-child(3)) {
		transition-delay: 0.15s;
	}

	/* LineHeading Dark Mode */
	:global(.dark-mode .section-title h2) {
		color: #ffffff !important;
	}
	
	:global(.dark-mode .section-title .text-purple-600) {
		color: #9D7FC1 !important;
	}

	/* Executive Summary Image Styles */
	.executive-summary-image {
		margin-top: 3rem;
		width: 100%;
		display: flex;
		justify-content: center;
		align-items: center;
		border-radius: 16px;
		overflow: hidden;
		box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
		transition: transform 0.3s ease, box-shadow 0.3s ease;
	}

	.executive-summary-image:hover {
		transform: translateY(-4px);
		box-shadow: 0 16px 48px rgba(0, 0, 0, 0.15);
	}

	.executive-summary-image img {
		width: 100%;
		height: auto;
		max-width: 100%;
		object-fit: cover;
		border-radius: 16px;
		transition: transform 0.3s ease;
	}

	.executive-summary-image:hover img {
		transform: scale(1.02);
	}

	/* Dark mode styles for executive summary image */
	:global(.dark-mode .executive-summary-image) {
		box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
	}

	:global(.dark-mode .executive-summary-image:hover) {
		box-shadow: 0 16px 48px rgba(0, 0, 0, 0.6);
	}

	/* Responsive design for executive summary image */
	@media (max-width: 768px) {
		.executive-summary-image {
			margin-top: 2rem;
			border-radius: 12px;
		}

		.executive-summary-image img {
			border-radius: 12px;
		}
	}

	/* Ensure ALL text elements change color in dark mode */
	:global(.dark-mode),
	:global(.dark-mode *),
	:global(.dark-mode h1),
	:global(.dark-mode h2),
	:global(.dark-mode h3),
	:global(.dark-mode h4),
	:global(.dark-mode h5),
	:global(.dark-mode h6),
	:global(.dark-mode p),
	:global(.dark-mode span),
	:global(.dark-mode div),
	:global(.dark-mode li),
	:global(.dark-mode ul),
	:global(.dark-mode ol),
	:global(.dark-mode td),
	:global(.dark-mode th),
	:global(.dark-mode label),
	:global(.dark-mode input),
	:global(.dark-mode textarea),
	:global(.dark-mode button),
	:global(.dark-mode a) {
		color: inherit;
	}

	/* Banner actions dark mode */
	:global(.dark-mode .banner-actions) {
		color: #ffffff;
	}

	/* Highlight numbers dark mode */
	:global(.dark-mode .highlight-number) {
		color: #4523AE;
	}

	/* Status dot remains green in both modes */
	:global(.dark-mode .status-dot) {
		background: #22c55e;
	}

	/* Additional text elements that might need explicit color */
	:global(.dark-mode .parallax-title),
	:global(.dark-mode .parallax-subtitle) {
		color: #ffffff;
	}

	/* Ensure TextDisperseBlack components work properly in dark mode */
	:global(.dark-mode .section-title *),
	:global(.dark-mode [class*="text-disperse"]) {
		color: #ffffff !important;
	}

	/* Override any hardcoded text colors */
	:global(.dark-mode [class*="text-black"]),
	:global(.dark-mode [class*="text-gray"]) {
		color: #e5e5e5 !important;
	}

	/* Carousel Container Styles */
	.carousel-container {
		display: flex;
		justify-content: center;
		align-items: center;
		padding: 2rem 0;
		overflow: visible;
	}

	/* Dark Mode for Carousel */
	:global(.dark-mode .carousel-container) {
		color: #ffffff;
	}

	/* Ensure carousel scales properly on mobile */
	@media (max-width: 768px) {
		.carousel-container {
			padding: 1rem 0;
		}
	}

	/* Metrics Grid Styles */
	.metrics-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		margin-top: 2rem;
	}

	.metric-card {
		text-align: center;
		padding: 2rem 1.5rem;
		background: white;
		border: 1px solid #e9ecef;
		border-radius: 16px;
		transition: all 0.3s ease;
	}

	.metric-card:hover {
		transform: translateY(-4px);
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
		border-color: #4523AE;
	}

	.metric-number {
		font-family: var(--font-display, 'Median', serif);
		font-size: clamp(2rem, 5vw, 3rem);
		font-weight: 400;
		color: #4523AE;
		margin-bottom: 0.5rem;
	}

	.metric-label {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		font-weight: 600;
		color: #1B1B1B;
		margin-bottom: 0.5rem;
	}

	.metric-description {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 0.875rem;
		color: #666;
	}

	/* Architecture Content Styles */
	.architecture-content {
		display: flex;
		flex-direction: column;
		gap: 3rem;
		margin-top: 2rem;
	}

	.architecture-item {
		display: grid;
		gap: 1.5rem;
		padding: 2rem 0;
		border-bottom: 1px solid #e9ecef;
	}

	@media (min-width: 1024px) {
		.architecture-item {
			grid-template-columns: 1fr 4fr;
			gap: 3rem;
		}
	}

	.architecture-item:last-child {
		border-bottom: none;
	}

	.architecture-number {
		font-family: var(--font-display, 'Median', serif);
		font-size: 2rem;
		font-weight: 400;
		color: #4523AE;
		text-align: center;
	}

	@media (min-width: 1024px) {
		.architecture-number {
			text-align: right;
		}
	}

	.architecture-details {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.architecture-details h3 {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.5rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0;
	}

	.architecture-subtitle {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		font-weight: 500;
		color: #4523AE;
		margin-bottom: 0.5rem;
	}

	.architecture-details p {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.6;
		color: #666;
		margin: 0;
	}

	/* Development Timeline Styles */
	.development-timeline {
		display: flex;
		flex-direction: column;
		gap: 2rem;
		margin-top: 2rem;
	}

	.timeline-item {
		display: grid;
		gap: 1.5rem;
		padding: 2rem;
		background: white;
		border: 1px solid #e9ecef;
		border-radius: 16px;
		transition: all 0.3s ease;
	}

	@media (min-width: 768px) {
		.timeline-item {
			grid-template-columns: 150px 1fr;
			gap: 2rem;
		}
	}

	.timeline-item:hover {
		transform: translateY(-2px);
		box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
		border-color: #4523AE;
	}

	.timeline-phase {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 0.875rem;
		font-weight: 600;
		color: #4523AE;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		text-align: center;
	}

	@media (min-width: 768px) {
		.timeline-phase {
			text-align: left;
		}
	}

	.timeline-content h3 {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.25rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 0.75rem 0;
	}

	.timeline-content p {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.6;
		color: #666;
		margin: 0;
	}

	/* Achievements Grid Styles */
	.achievements-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		margin-top: 2rem;
	}

	.achievement-item {
		padding: 2rem;
		background: white;
		border: 1px solid #e9ecef;
		border-radius: 16px;
		transition: all 0.3s ease;
	}

	.achievement-item:hover {
		transform: translateY(-4px);
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
		border-color: #4523AE;
	}

	.achievement-item h3 {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.25rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 1.5rem 0;
	}

	.achievement-item ul {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}

	.achievement-item li {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 0.95rem;
		line-height: 1.5;
		color: #666;
		display: flex;
		align-items: flex-start;
		gap: 0.75rem;
	}

	.achievement-item li::before {
		content: '';
		display: inline-block;
		width: 16px;
		height: 16px;
		background: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' width='16' height='16' viewBox='0 0 24 24' fill='none' stroke='%234523AE' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpath d='M20 6 9 17l-5-5'/%3e%3c/svg%3e") no-repeat center;
		background-size: contain;
		margin-top: 0.1rem;
		flex-shrink: 0;
	}

	/* Live Application Styles */
	.live-app-content {
		margin-top: 2rem;
	}

	.app-info {
		display: grid;
		gap: 3rem;
	}

	@media (min-width: 1024px) {
		.app-info {
			grid-template-columns: 2fr 1fr;
			gap: 4rem;
		}
	}

	.app-details h3 {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.5rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 1rem 0;
	}

	.app-description {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		line-height: 1.6;
		color: #666;
		margin: 0 0 2rem 0;
	}

	.app-links {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.link-item {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.link-label {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 0.875rem;
		font-weight: 500;
		color: #666;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.link-url {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		font-weight: 500;
		color: #4523AE;
		text-decoration: none;
		transition: color 0.3s ease;
	}

	.link-url:hover {
		color: #2D1B69;
		text-decoration: underline;
	}

	.link-text {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		font-weight: 500;
		color: #1B1B1B;
	}

	.app-stats {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1.5rem;
	}

	.stat-item {
		text-align: center;
		padding: 1.5rem;
		background: white;
		border: 1px solid #e9ecef;
		border-radius: 12px;
		transition: all 0.3s ease;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.5rem;
	}

	.stat-icon {
		display: flex;
		align-items: center;
		justify-content: center;
		color: #4523AE;
		margin-bottom: 0.25rem;
	}

	.stat-item:hover {
		transform: translateY(-2px);
		box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
		border-color: #4523AE;
	}

	.stat-value {
		font-family: var(--font-display, 'Median', serif);
		font-size: 1.75rem;
		font-weight: 400;
		color: #4523AE;
		margin-bottom: 0.5rem;
	}

	.stat-label {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 0.875rem;
		font-weight: 500;
		color: #666;
		line-height: 1.3;
	}

	/* Dark Mode Styles for New Sections */
	:global(.dark-mode .metric-card),
	:global(.dark-mode .feature-card),
	:global(.dark-mode .info-card),
	:global(.dark-mode .timeline-item),
	:global(.dark-mode .achievement-item),
	:global(.dark-mode .stat-item) {
		background: #0a0816;
		border-color: rgba(51, 51, 51, 0.3);
	}

	:global(.dark-mode .metric-card:hover),
	:global(.dark-mode .feature-card:hover),
	:global(.dark-mode .info-card:hover),
	:global(.dark-mode .timeline-item:hover),
	:global(.dark-mode .achievement-item:hover),
	:global(.dark-mode .stat-item:hover) {
		background: #0f0a1a;
		border-color: rgba(69, 35, 174, 0.6);
	}

	:global(.dark-mode .metric-label),
	:global(.dark-mode .architecture-details h3),
	:global(.dark-mode .timeline-content h3),
	:global(.dark-mode .achievement-item h3),
	:global(.dark-mode .app-details h3),
	:global(.dark-mode .link-text) {
		color: #ffffff !important;
	}

	:global(.dark-mode .metric-description),
	:global(.dark-mode .architecture-details p),
	:global(.dark-mode .timeline-content p),
	:global(.dark-mode .achievement-item li),
	:global(.dark-mode .app-description),
	:global(.dark-mode .link-label),
	:global(.dark-mode .stat-label) {
		color: #cccccc !important;
	}

	:global(.dark-mode .metric-number),
	:global(.dark-mode .architecture-number),
	:global(.dark-mode .architecture-subtitle),
	:global(.dark-mode .timeline-phase),
	:global(.dark-mode .stat-value) {
		color: #4523AE !important;
	}

	:global(.dark-mode .link-url) {
		color: #ffffff !important;
	}

	:global(.dark-mode .stat-icon) {
		color: #4523AE !important;
	}

	:global(.dark-mode .link-label svg) {
		color: #cccccc !important;
	}

	:global(.dark-mode .architecture-item),
	:global(.dark-mode .architecture-item:last-child) {
		border-bottom-color: #333333;
	}

	:global(.dark-mode .architecture-item:last-child) {
		border-bottom: none;
	}

	/* Ensure link hover states work in dark mode */
	:global(.dark-mode .link-url:hover) {
		color: #c4b5fd !important;
	}

	:global(.dark-mode a) {
		color: #9b7ae8 !important;
	}

	:global(.dark-mode a:hover) {
		color: #c4b5fd !important;
	}

	/* Make sure all list items and bullets are visible */
	:global(.dark-mode .achievement-item li::before) {
		background: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' width='16' height='16' viewBox='0 0 24 24' fill='none' stroke='%23b8c4e6' stroke-width='1' stroke-linecap='round' stroke-linejoin='round' opacity='0.6'%3e%3cpath d='M20 6 9 17l-5-5'/%3e%3c/svg%3e") no-repeat center !important;
		background-size: contain !important;
	}

	/* Comprehensive text color override for all sections */
	:global(.dark-mode .content-section *) {
		color: inherit;
	}

	/* Force specific text elements to proper colors */
	:global(.dark-mode .content-section h1),
	:global(.dark-mode .content-section h2),
	:global(.dark-mode .content-section h3),
	:global(.dark-mode .content-section h4),
	:global(.dark-mode .content-section h5),
	:global(.dark-mode .content-section h6) {
		color: #f0f4ff !important;
	}

	/* ProfileCard Dark Mode Styles - Following DarkMode.md methodology */
	:global(.dark-mode .profile-card) {
		background: #050112 !important;
		border-color: rgba(255, 255, 255, 0.1) !important;
		color: #ffffff !important;
	}

	:global(.dark-mode .profile-name) {
		color: #ffffff !important;
	}

	:global(.dark-mode .profile-bio) {
		color: rgba(255, 255, 255, 0.8) !important;
	}

	:global(.dark-mode .profile-divider) {
		background: rgba(255, 255, 255, 0.1) !important;
	}

	:global(.dark-mode .social-button) {
		background: rgba(255, 255, 255, 0.1) !important;
		border-color: rgba(255, 255, 255, 0.1) !important;
	}

	:global(.dark-mode .social-button:hover) {
		background: rgba(255, 255, 255, 0.2) !important;
	}

	:global(.dark-mode .social-icon) {
		color: rgba(255, 255, 255, 0.7) !important;
	}

	:global(.dark-mode .social-button:hover .social-icon) {
		color: rgba(255, 255, 255, 0.9) !important;
	}

	:global(.dark-mode .content-section p),
	:global(.dark-mode .content-section span),
	:global(.dark-mode .content-section div),
	:global(.dark-mode .content-section li) {
		color: #b8c4e6 !important;
	}

	/* Ultimate fallback - override any remaining dark text colors */
	:global(.dark-mode *[style*="color: rgb(0, 0, 0)"]),
	:global(.dark-mode *[style*="color: black"]),
	:global(.dark-mode *[style*="color: #000"]) {
		color: #ffffff !important;
	}

	/* Responsive adjustments for new sections */
	@media (max-width: 768px) {
		.metrics-grid {
			grid-template-columns: repeat(2, 1fr);
			gap: 1rem;
		}

		.metric-card {
			padding: 1.5rem 1rem;
		}

		.architecture-item {
			padding: 1.5rem 0;
		}

		.achievements-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.achievement-item {
			padding: 1.5rem;
		}

		.app-stats {
			grid-template-columns: 1fr;
			gap: 1rem;
		}
	}

	@media (max-width: 480px) {
		.metrics-grid {
			grid-template-columns: 1fr;
		}

		.metric-number {
			font-size: 2rem;
		}

		.architecture-number {
			font-size: 1.5rem;
		}
	}

	/* Code Section Styles */
	.code-section {
		margin-bottom: clamp(2rem, 4vw, 3rem);
		padding: 100px 100px 0px 100px;
		
	}

	.code-section-title {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: clamp(1rem, 2.5vw, 1.25rem);
		font-weight: 600;
		color: #1B1B1B;
		margin-bottom: clamp(0.5rem, 1vw, 0.75rem);
	}

	.code-section-description {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: clamp(0.8rem, 1.8vw, 0.9rem);
		line-height: 1.5;
		color: #666;
		margin-bottom: clamp(1rem, 2vw, 1.5rem);
	}

	/* Dark mode styles for code sections */
	:global(.dark-mode) .code-section-title {
		color: #ffffff;
	}

	:global(.dark-mode) .code-section-description {
		color: #cccccc;
	}

	/* Responsive adjustments for code sections */
	@media (max-width: 768px) {
		.code-section {
			margin-bottom: 3rem;
			padding: 100px 0px 0px 0px;
		}

		.code-section-title {
			font-size: 1.25rem;
		}

		.code-section-description {
			font-size: 0.9rem;
		}
	}

	@media (max-width: 480px) {
		.code-section {
			margin-bottom: 2rem;
		}

		.code-section-title {
			font-size: 1.125rem;
		}
	}

	/* Reviews Section Styles - Light Mode */
	:global(.reviews-section) {
		width: 100%;
		max-width: 1800px;
		margin: 0 auto;
		background-color: white;
		background-image: 
			url('https://pub-3ed7c563bcaa4c7c8ed703c87bbc1631.r2.dev/EarthSphere.svg'),
			linear-gradient(rgba(0, 0, 0, 0.020) 1px, transparent 1px), 
			linear-gradient(90deg, rgba(0, 0, 0, 0.020) 1px, transparent 1px);
		background-size: 
			clamp(300px, 40vw, 500px),
			20px 20px,
			20px 20px;
		background-position: 
			center center,
			0 0,
			0 0;
		background-repeat: 
			no-repeat,
			repeat,
			repeat;
		position: relative;
		overflow: hidden;
		transition: background-color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94),
		           background-image 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	/* Dark Mode - Reviews Section */
	:global(.dark-mode .reviews-section) {
		background-color: #050112;
		background-image: 
			url('https://pub-3ed7c563bcaa4c7c8ed703c87bbc1631.r2.dev/EarthSphere.svg'),
			linear-gradient(rgba(248, 244, 255, 0.03) 1px, transparent 1px), 
			linear-gradient(90deg, rgba(248, 244, 255, 0.03) 1px, transparent 1px);
	}

	/* Gradiente sutil en los costados para viewports grandes */
	@media (min-width: 1801px) {
		:global(.reviews-section::before) {
			content: '';
			position: absolute;
			top: 0;
			left: 0;
			right: 0;
			bottom: 0;
			background: linear-gradient(
				to right,
				rgba(255, 255, 255, 0.4) 0%,
				transparent 15%,
				transparent 85%,
				rgba(255, 255, 255, 0.4) 100%
			);
			pointer-events: none;
			z-index: 5;
			transition: background 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
		}

		/* Dark Mode - Gradiente sutil */
		:global(.dark-mode .reviews-section::before) {
			background: linear-gradient(
				to right,
				rgba(5, 1, 18, 0.6) 0%,
				transparent 15%,
				transparent 85%,
				rgba(5, 1, 18, 0.6) 100%
			);
		}
	}

	/* Ocultar brush en viewports mayores a 1500px */
	@media (min-width: 1501px) {
		.brush-svg-overlay {
			display: none !important;
		}
	}

	.reviews-section__container {
		width: 100%;
		max-width: none;
		margin: 0;
		padding: 0;
	}

	.reviews-section__header {
		position: relative;
		z-index: 10;
		text-align: center;
		margin-bottom: 3rem;
		padding-top: 2rem;
	}

	:global(.reviews-section__title) {
		width: 100%;
		height: 100%;
		justify-content: flex-start;
		align-items: flex-start;
		gap: 20px;
		display: inline-flex;
		margin-bottom: 1rem;
	}

	:global(.title-clients) {
		text-box-trim: trim-both;
		text-box-edge: cap alphabetic;
		color: black;
		font-size: 64px;
		font-family: 'Playfair Display', serif;
		font-style: italic;
		font-weight: 400;
		line-height: 64px;
		word-wrap: break-word;
		transition: color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	:global(.title-reviews) {
		text-box-trim: trim-both;
		text-box-edge: cap alphabetic;
		color: black;
		font-size: 64px;
		font-family: 'Inter', sans-serif;
		font-weight: 400;
		line-height: 64px;
		word-wrap: break-word;
		transition: color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	:global(.reviews-section__subtitle) {
		font-family: var(--font-primary);
		font-size: clamp(1rem, 2.5vw, 1.25rem);
		color: #64748b;
		max-width: 600px;
		margin: 0 auto;
		line-height: var(--leading-relaxed);
		transition: color 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	/* Dark Mode Styles */
	:global(.dark-mode .title-clients) {
		color: #f8f4ff;
	}

	:global(.dark-mode .title-reviews) {
		color: #f8f4ff;
	}

	:global(.dark-mode .reviews-section__subtitle) {
		color: #c4b5fd;
	}

	.reviews-section__reviews {
		position: relative;
		width: 100%;
		min-height: 1000px;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		overflow: hidden;
	}

	.reviews-glsl-background {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		min-height: 800px;
		z-index: 0;
		pointer-events: none;
		opacity: 0.7;
		overflow: hidden;
	}

	/* GLSL Hills specific styling */
	.reviews-glsl-background .reviews-hills-bg {
		width: 100%;
		height: 100%;
		filter: blur(0.5px);
	}

	.reviews-glsl-background .glsl-hills-wrapper {
		width: 100%;
		height: 100%;
	}

	/* Ensure ReviewsIsland is above background and perfectly centered */
	.reviews-section__reviews > astro-island[component-url*="ReviewsIsland"] {
		position: relative;
		z-index: 10;
		width: 100%;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	@media (max-width: 768px) {
		.reviews-section {
			padding: 2rem 0;
		}

		.reviews-section__container {
			padding: 0;
		}

		.reviews-section__header {
			margin-bottom: 2rem;
			padding-top: 1rem;
		}

		.title-clients,
		.title-reviews {
			font-size: 48px;
			line-height: 48px;
		}

		.reviews-section__title {
			gap: 15px;
		}

		.reviews-glsl-background {
			opacity: 0.5;
			min-height: 600px;
		}

		.reviews-glsl-background .reviews-hills-bg {
			filter: blur(1px);
		}
	}

	@media (max-width: 480px) {
		.title-clients,
		.title-reviews {
			font-size: 36px;
			line-height: 36px;
		}

		.reviews-section__title {
			gap: 10px;
			flex-direction: column;
			align-items: center;
			text-align: center;
		}

		.reviews-glsl-background {
			opacity: 0.3;
			min-height: 400px;
		}

		.reviews-glsl-background .reviews-hills-bg {
			filter: blur(1.5px);
		}
	}

	/* Sticky Scroll Effect Styles */
	.sticky-scroll-wrapper {
		position: relative;
		width: 100%;
		height: 500vh; /* Aún más alto para permitir animación progresiva de cards */
	}

	.sticky-reviews {
		position: sticky;
		top: 0;
		height: 100vh;
		width: 100%;
		z-index: 10;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	/* Reviews section styling for sticky */
	.sticky-reviews .reviews-section__container {
		height: 100%;
		width: 100%;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.sticky-reviews .reviews-section__reviews {
		position: relative;
		width: 100%;
		height: 100%;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
	}

	/* Background positioning for sticky reviews */
	.sticky-reviews .reviews-glsl-background {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: 1;
	}

	/* Header positioning for sticky reviews */
	.sticky-reviews .reviews-section__header {
		position: relative;
		z-index: 10;
		margin-bottom: 3rem;
		text-align: center;
	}

	/* Reviews island positioning for sticky reviews */
	.sticky-reviews .reviews-section__reviews > astro-island[component-url*="ReviewsIsland"] {
		position: relative;
		z-index: 10;
		width: 100%;
		max-width: 1400px;
		margin: 0 auto;
	}

	/* Responsive adjustments for sticky scroll */
	@media (max-width: 768px) {
		.sticky-scroll-wrapper {
			height: 400vh; /* Más altura en tablet para animación progresiva */
		}
		
		.sticky-reviews {
			height: 80vh;
		}
		
		.sticky-reviews .reviews-section__header {
			margin-bottom: 2rem;
		}
	}

	@media (max-width: 480px) {
		.sticky-scroll-wrapper {
			height: 350vh; /* Más altura en móvil para animación progresiva */
		}
		
		.sticky-reviews {
			height: 70vh;
		}
		
		.sticky-reviews .reviews-section__header {
			margin-bottom: 1.5rem;
		}
	}

	/* Smooth transitions for sticky effect */
	.sticky-reviews {
		transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	/* Accessibility: Respect reduced motion */
	@media (prefers-reduced-motion: reduce) {
		.sticky-reviews {
			transition: none;
		}
	}

</style>