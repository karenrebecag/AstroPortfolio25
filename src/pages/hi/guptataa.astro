---
export const prerender = true;

import Layout from '../../layouts/Layout.astro';
import HeaderPill from '../../components/modules/Header/HeaderPill.astro';
import { TextDisperseBlack } from '../../components/ui/TextDisperseBlack.tsx';
import { InView } from '../../components/ui/InView.tsx';
import { DarkModeContainer } from '../../components/ui/DarkModeContainer.tsx';
import ShareBar from '../../components/modules/ShareBar/ShareBar.astro';
import { translations } from '../../i18n/translations.js';
import '../../styles/global.css';

const t = translations.hi;
const privacyData = t.privacy;
---

<Layout title="Karen Ortiz - गोपनीयता नीति" lang="hi">
	<HeaderPill />

	<DarkModeContainer client:load>
		<main class="main-content" id="main-content">
		<section class="content-section privacy-section" id="privacy-policy">
			<div class="section-container">
				<div class="section-header">
					<h2 class="section-title" id="privacy-message">
						<TextDisperseBlack client:idle text={privacyData.title} />
					</h2>
					<div class="section-label">{privacyData.subtitle}</div>
				</div>

				<InView client:idle
					variants={{
						hidden: { opacity: 0, y: 30, filter: "blur(2px)" },
						visible: { opacity: 1, y: 0, filter: "blur(0px)" },
					}}
					viewOptions={{ margin: "0px 0px -100px 0px" }}
					transition={{ duration: 0.6, ease: [0.25, 0.46, 0.45, 0.94] }}
				>
					<div class="content-grid">
						<div class="content-main">
							<p class="content-description">
								{privacyData.description}
							</p>

							<div class="content-highlights">
								{privacyData.sections.map((section) => (
									<div class="highlight-item">
										<div class="highlight-number">{section.number}</div>
										<div class="highlight-content">
											<h3>{section.title}</h3>
											<p>{section.content}</p>
										</div>
									</div>
								))}
							</div>

							<div class="privacy-details">
								<div class="detail-section">
									<h3>{privacyData.details.title}</h3>
									<p>{privacyData.details.intro}</p>
									
									<h4>{privacyData.details.architecture.title}</h4>
									<p>{privacyData.details.architecture.content}</p>
									
									<h4>{privacyData.details.moderation.title}</h4>
									<p>{privacyData.details.moderation.content}</p>
									
									<h4>{privacyData.details.principles.title}</h4>
									<p>{privacyData.details.principles.content}</p>
									
									<h4>{privacyData.details.thirdParty.title}</h4>
									<p>{privacyData.details.thirdParty.content}</p>
									
									<h4>{privacyData.details.retention.title}</h4>
									<p>{privacyData.details.retention.content}</p>
									
									<h4>{privacyData.details.contact.title}</h4>
									<p>{privacyData.details.contact.content}</p>
									
									<p class="last-updated"><strong>{privacyData.details.lastUpdated}</strong></p>
								</div>
							</div>
						</div>
					</div>
				</InView>
			</div>
		</section>

		</main>
	</DarkModeContainer>
</Layout>

<style>
	.main-content {
		background: #ececec;
		width: 100%;
		padding: 0;
	}

	.content-section {
		padding: 6rem 2rem;
		border-bottom: 1px solid #f0f0f0;
		background-image:
			linear-gradient(rgba(0, 0, 0, 0.03) 1px, transparent 1px),
			linear-gradient(90deg, rgba(0, 0, 0, 0.03) 1px, transparent 1px);
		background-size: 20px 20px;
		position: relative;
	}

	.privacy-section {
		padding-top: 8rem;
	}

	.section-container {
		max-width: 1200px;
		margin: 0 auto;
		width: 100%;
	}

	.section-header {
		margin-bottom: 4rem;
		text-align: center;
	}

:global(.section-title) {
		font-family: var(--font-display, 'Median', serif);
		font-size: clamp(2.5rem, 6vw, 4rem);
		font-weight: 400;
		line-height: 0.9;
		margin-bottom: 1rem;
		color: #1B1B1B;
	}

:global(.section-label) {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 0.875rem;
		font-weight: 500;
		color: #666;
		text-transform: uppercase;
		letter-spacing: 0.1em;
	}

	.content-grid {
		display: grid;
		grid-template-columns: 1fr;
		gap: 4rem;
		align-items: start;
	}

	.content-main {
		display: flex;
		flex-direction: column;
		gap: 3rem;
	}

:global(.content-description) {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		line-height: 1.6;
		color: #333;
		margin: 0;
	}

	.content-highlights {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	.highlight-item {
		display: flex;
		gap: 1.5rem;
		align-items: flex-start;
	}

:global(.highlight-number) {
		font-family: var(--font-display, 'Median', serif);
		font-size: 1.5rem;
		font-weight: 400;
		color: #4523AE;
		min-width: 3rem;
		text-align: center;
	}

:global(.highlight-content h3) {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 0.5rem 0;
	}

:global(.highlight-content p) {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.5;
		color: #666;
		margin: 0;
	}

:global(.privacy-details) {
		margin-top: 3rem;
		padding: 2rem;
		background: white;
		border: 1px solid #e9ecef;
		border-radius: 16px;
		box-shadow: 0 4px 16px rgba(0, 0, 0, 0.05);
	}

:global(.detail-section h3) {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.25rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 0 0 1rem 0;
	}

:global(.detail-section h4) {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1.125rem;
		font-weight: 600;
		color: #1B1B1B;
		margin: 2rem 0 0.75rem 0;
	}

:global(.detail-section p) {
		font-family: var(--font-primary, 'InterTight', sans-serif);
		font-size: 1rem;
		line-height: 1.6;
		color: #666;
		margin: 0 0 1rem 0;
	}

:global(.last-updated) {
		margin-top: 2rem;
		padding-top: 1rem;
		border-top: 1px solid #e9ecef;
		font-size: 0.875rem !important;
		color: #999 !important;
	}

	@media (max-width: 768px) {
		.content-section {
			padding: 5.6rem 1.4rem;
		}

		.privacy-section {
			padding-top: 11.2rem;
		}

		.section-header {
			margin-bottom: 3rem;
		}

		.highlight-item {
			gap: 1rem;
		}

		.highlight-number {
			min-width: 2rem;
			font-size: 1.25rem;
		}

		.privacy-details {
			padding: 1.5rem;
		}
	}

	:global(.dark-mode .main-content) {
		background: #050112;
		color: #ffffff;
	}

	:global(.dark-mode .content-section) {
		border-bottom-color: #333333;
		background-image:
			linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
			linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
	}

	:global(.dark-mode .section-title) {
		color: #ffffff;
	}

	:global(.dark-mode .section-label) {
		color: #cccccc;
	}

	:global(.dark-mode .content-description) {
		color: #e5e5e5;
	}

	:global(.dark-mode .highlight-content h3) {
		color: #ffffff;
	}

	:global(.dark-mode .highlight-content p) {
		color: #cccccc;
	}

	:global(.dark-mode .privacy-details) {
		background: #0a0816;
		border-color: rgba(51, 51, 51, 0.3);
	}

	:global(.dark-mode .detail-section h3),
	:global(.dark-mode .detail-section h4) {
		color: #ffffff;
	}

	:global(.dark-mode .detail-section p) {
		color: #cccccc;
	}

	:global(.dark-mode .last-updated) {
		border-top-color: #333333;
		color: #999999 !important;
	}
</style>
