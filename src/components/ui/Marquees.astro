---
// Marquees.astro
import MarqueesIsland from './MarqueesIsland.tsx';
---

<section class="marquees-section">
  <div class="marquees-section-container">
    <div class="marquees-section-wrapper">
      <MarqueesIsland client:visible />
    </div>
  </div>
</section>

<style>
  :global(.marquees-section) {
    width: 100%;
    background: #FEFEFE;
    background-image: 
      linear-gradient(rgba(0, 0, 0, 0.05) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0, 0, 0, 0.05) 1px, transparent 1px);
    background-size: 40px 40px;
    overflow: hidden;
    position: relative;
    transition: background 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94), background-image 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .marquees-section-container {
    width: 100%;
    display: flex;
    justify-content: center;
  }

  .marquees-section-wrapper {
    width: 100%;
    max-width: 1800px;
    position: relative;
    overflow: hidden;
  }

  /* Gradientes laterales para pantallas grandes */
  @media (min-width: 2560px) {
    .marquees-section-wrapper::before,
    .marquees-section-wrapper::after {
      content: '';
      position: absolute;
      top: 0;
      bottom: 0;
      width: 120px;
      z-index: 10;
      pointer-events: none;
    }

    .marquees-section-wrapper::before {
      left: 0;
      background: linear-gradient(to right, #FEFEFE, transparent);
      transition: background 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }

    .marquees-section-wrapper::after {
      right: 0;
      background: linear-gradient(to left, #FEFEFE, transparent);
      transition: background 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }
  }

  :global(.marquees-container) {
    position: relative;
    width: 100%;
    height: 200px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  :global(.marquee-wrapper) {
    position: absolute;
    width: 200%;
    height: 80px;
    display: flex;
    align-items: center;
    transform-origin: center;
    overflow: hidden;
  }

  :global(.first-marquee) {
    background: #7256c6;
    transform: rotate(5deg);
    z-index: 1;
  }

  :global(.second-marquee) {
    background: #000000;
    transform: rotate(-5deg);
    z-index: 2;
  }

  @media (max-width: 768px) {
    .marquees-section {
      background-size: 20px 20px;
    }
    
    :global(.marquees-container) {
      height: 150px;
    }
    
    :global(.marquee-wrapper) {
      height: 60px;
      width: 300%;
    }
    
    :global(.first-marquee) {
      transform: rotate(5deg);
    }

    :global(.second-marquee) {
      transform: rotate(-5deg);
    }
  }

  /* Accessibility: Respect user's motion preferences */
  @media (prefers-reduced-motion: reduce) {
    :global(.marquee-wrapper) {
      animation: none;
    }
    
    :global(.marquees-container) {
      /* Provide static fallback when motion is reduced */
      overflow: hidden;
    }
  }

  /* Dark Mode Styles - Following Documentation Pattern */
  :global(.dark-mode .marquees-section) {
    background: #020009;
    background-image: 
      linear-gradient(rgba(112, 11, 108, 0.15) 1px, transparent 1px),
      linear-gradient(90deg, rgba(112, 11, 108, 0.15) 1px, transparent 1px);
  }

  /* Dark mode gradients for large screens */
  @media (min-width: 2560px) {
    :global(.dark-mode) .marquees-section-wrapper::before {
      background: linear-gradient(to right, #020009, transparent);
    }

    :global(.dark-mode) .marquees-section-wrapper::after {
      background: linear-gradient(to left, #020009, transparent);
    }
  }

  /* Marquee backgrounds with enhanced contrast for dark mode */
  :global(.dark-mode .first-marquee) {
    background: #9D7FC1; /* Purple brand color for better contrast */
  }

  :global(.dark-mode .second-marquee) {
    background: #4523AE; /* Darker purple for variety */
  }
</style>
