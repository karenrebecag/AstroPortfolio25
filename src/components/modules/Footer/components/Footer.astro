---
import { StickyFooter } from './StickyFooter';
import { getLangFromUrl } from '../../../../i18n/utils.js';

const lang = getLangFromUrl(Astro.url);
---

<StickyFooter client:visible lang={lang} />