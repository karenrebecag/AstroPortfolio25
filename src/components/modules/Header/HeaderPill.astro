---
import MainButton from './MainButton.astro';
import LanguageSelectorWrapper from './LanguageSelectorWrapper.astro';
import { getLangFromUrl, t, languageOptions, useTranslatedPath } from '../../../i18n/utils.js';
import styles from './HeaderPill.module.css';

const lang = getLangFromUrl(Astro.url);
---

<div class={styles['header-pill']}>


  <div class={styles['header-wrapper']} id="headerWrapper">
    <header class={styles['header']} id="mainHeader">
      <div class={styles['logo-section']}>
        <button class={styles['back-btn']} id="backBtn" data-cursor-text="Go Back" aria-label="Go back to home page">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="m15 18-6-6 6-6"/>
          </svg>
        </button>
        <div class={styles['logo']} id="logoBtn" data-cursor-text="Go Home">KAREN ORTIZ</div>
      </div>

      <div class={styles['header-right-section']}>
        <div class={styles['header-actions-wrapper']}>
          <div class={styles['contact-btn-wrapper']}>
            <a href="/#contact" class={styles['contact-link']} data-cursor-text="Get In Touch">
              <MainButton text={t('nav.contact', lang)} />
            </a>
          </div>
          <LanguageSelectorWrapper />
        </div>

        <div class={styles['dark-mode-toggle-wrapper']}>
          <div id="darkModeToggle"></div>
        </div>
      </div>
    </header>
  </div>
</div>

<div class={styles['mobile-menu']} id="mobileMenu">
  <div class={styles['mobile-menu-content']}>
    <button class={styles['mobile-close-btn']} id="mobileCloseBtn" data-cursor-text="Close Menu">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-x">
        <path d="m18 6-12 12"/>
        <path d="m6 6 12 12"/>
      </svg>
    </button>

  
  </div>
</div>



<script>
  document.addEventListener('DOMContentLoaded', () => {
    const headerPill = document.querySelector('[class*="header-pill"]');
    let lastScrollY = window.scrollY;
    let isScrolling = false;

    const handleScroll = () => {
      if (!isScrolling) {
        window.requestAnimationFrame(() => {
          const currentScrollY = window.scrollY;
          const scrollDifference = currentScrollY - lastScrollY;

          if (!headerPill) return;

          if (currentScrollY > 80) {
            headerPill.classList.add('scrolled');
          } else {
            headerPill.classList.remove('scrolled');
          }

          if (scrollDifference > 5 && currentScrollY > 100) {
            if (!headerPill.classList.contains('hidden')) {
              headerPill.classList.add('hidden');

              const allHeaders = document.querySelectorAll('[class*="header-pill"]');
              allHeaders.forEach((header) => {
                (header as HTMLElement).style.transition = 'transform 0.4s cubic-bezier(0.4, 0.0, 0.2, 1), opacity 0.4s cubic-bezier(0.4, 0.0, 0.2, 1)';
                (header as HTMLElement).style.transform = 'translateX(-50%) translateY(-100%)';
                (header as HTMLElement).style.opacity = '0';
              });

              setTimeout(() => {
                allHeaders.forEach((header) => {
                  (header as HTMLElement).style.display = 'none';
                });
              }, 400);
            }
          } else if (scrollDifference < -2 || currentScrollY < 80) {
            if (headerPill.classList.contains('hidden')) {
              headerPill.classList.remove('hidden');

              const allHeaders = document.querySelectorAll('[class*="header-pill"]');
              allHeaders.forEach((header) => {
                (header as HTMLElement).style.display = '';
                (header as HTMLElement).style.transition = 'transform 0.5s cubic-bezier(0.23, 1, 0.32, 1), opacity 0.5s cubic-bezier(0.23, 1, 0.32, 1)';
                (header as HTMLElement).style.transform = 'translateX(-50%) translateY(-60px)';
                (header as HTMLElement).style.opacity = '0';
              });

              requestAnimationFrame(() => {
                requestAnimationFrame(() => {
                  allHeaders.forEach((header) => {
                    (header as HTMLElement).style.transform = 'translateX(-50%) translateY(0%)';
                    (header as HTMLElement).style.opacity = '1';
                  });
                });
              });
            }
          }

          lastScrollY = currentScrollY;
          isScrolling = false;
        });
      }
      isScrolling = true;
    };

    window.addEventListener('scroll', handleScroll, { passive: true });

    const allMenuBtns = document.querySelectorAll('[id*="menuBtn"]');
    const mobileMenu = document.querySelector('[id="mobileMenu"]');
    const mobileNav = mobileMenu?.querySelector('nav');
    const mobileNavLinks = mobileNav?.querySelectorAll('a');
    const contactBtn = mobileNav?.querySelector('a[href*="contact"]');

    let isOpen = false;

    const openMenu = () => {
      isOpen = true;
      mobileMenu?.classList.add('open');

      document.body.style.overflow = 'hidden';
      document.body.style.position = 'fixed';
      document.body.style.width = '100%';

      // Reset initial states
      if (mobileNav) (mobileNav as HTMLElement).style.transform = 'translateY(30px)';
      mobileNavLinks?.forEach(link => {
        const linkEl = link as HTMLElement;
        linkEl.style.opacity = '0';
        linkEl.style.transform = 'translateY(20px)';
      });
      if (contactBtn) {
        (contactBtn as HTMLElement).style.opacity = '0';
        (contactBtn as HTMLElement).style.transform = 'translateY(20px)';
      }

      setTimeout(() => {
        if (mobileNav) (mobileNav as HTMLElement).style.transform = 'translateY(0px)';
        mobileNavLinks?.forEach((link, index) => {
          const linkEl = link as HTMLElement;
          setTimeout(() => {
            linkEl.style.opacity = '1';
            linkEl.style.transform = 'translateY(0px)';
          }, 100 + (index * 100));
        });
        if (contactBtn) {
          setTimeout(() => {
            (contactBtn as HTMLElement).style.opacity = '1';
            (contactBtn as HTMLElement).style.transform = 'translateY(0px)';
          }, 500);
        }
      }, 100);
    };

    const closeMenu = () => {
      isOpen = false;

      mobileNavLinks?.forEach((link, index) => {
        const linkEl = link as HTMLElement;
        setTimeout(() => {
          linkEl.style.opacity = '0';
          linkEl.style.transform = 'translateY(-20px)';
        }, index * 50);
      });

      if (contactBtn) {
        setTimeout(() => {
          (contactBtn as HTMLElement).style.opacity = '0';
          (contactBtn as HTMLElement).style.transform = 'translateY(-20px)';
        }, 100);
      }

      if (mobileNav) {
        setTimeout(() => {
          (mobileNav as HTMLElement).style.transform = 'translateY(30px)';
        }, 200);
      }

      setTimeout(() => {
        mobileMenu?.classList.remove('open');

        document.body.style.overflow = '';
        document.body.style.position = '';
        document.body.style.width = '';
      }, 500);
    };

    allMenuBtns.forEach((btn) => {
      btn.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();
        if (isOpen) {
          closeMenu();
        } else {
          openMenu();
        }
      });
    });

    const mobileCloseBtn = document.querySelector('[id="mobileCloseBtn"]');
    mobileCloseBtn?.addEventListener('click', () => {
      if (isOpen) {
        closeMenu();
      }
    });

    mobileMenu?.addEventListener('click', (e) => {
      const target = e.target as HTMLElement;
      if (target && (target.tagName === 'A' || target.classList.contains('mobile-contact-btn'))) {
        closeMenu();
      }
    });

    mobileMenu?.addEventListener('click', (e) => {
      if (e.target === mobileMenu) {
        closeMenu();
      }
    });

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && isOpen) {
        closeMenu();
      }
    });

    // Cache scroll positions to avoid repeated calculations
    const scrollCache = new Map();
    
    const smoothScrollTo = (targetId: string) => {
      let target = document.getElementById(targetId);

      if (!target) {
        setTimeout(() => {
          target = document.getElementById(targetId);
          if (target) {
            scrollToTarget(target);
          }
        }, 100);
        return;
      }

      scrollToTarget(target);
    };

    const scrollToTarget = (target: HTMLElement) => {
      // Use cached position if available
      const cacheKey = target.id;
      if (scrollCache.has(cacheKey)) {
        const cachedPosition = scrollCache.get(cacheKey);
        window.scrollTo({
          top: cachedPosition,
          behavior: 'smooth'
        });
        return;
      }

      // Calculate once and cache - use offsetTop for better performance
      const headerHeight = 80;
      const targetPosition = target.offsetTop - headerHeight - 20;
      
      // Cache the position
      scrollCache.set(cacheKey, targetPosition);
      
      window.scrollTo({
        top: targetPosition,
        behavior: 'smooth'
      });
    };

    const logoBtn = document.querySelector('[id="logoBtn"]');
    logoBtn?.addEventListener('click', () => {
      // Check if we're on the home page
      const currentPath = window.location.pathname;
      if (currentPath === '/' || currentPath === '/index.html') {
        // We're on home page, scroll to top
        window.scrollTo({
          top: 0,
          behavior: 'smooth'
        });
      } else {
        // We're on a different page, navigate to home
        window.location.href = '/';
      }
    });

    const backBtn = document.querySelector('[id="backBtn"]');
    backBtn?.addEventListener('click', () => {
      window.location.href = '/';
    });

    const desktopNavLinks = document.querySelectorAll('[id="mainHeader"] nav a');
    desktopNavLinks.forEach(link => {
      link.addEventListener('click', (e) => {
        e.preventDefault();
        const href = link.getAttribute('href');
        if (href && href.startsWith('#')) {
          const targetId = href.substring(1);
          smoothScrollTo(targetId);
        }
      });
    });

    const mobileScrollLinks = document.querySelectorAll('[id="mobileMenu"] a');
    mobileScrollLinks.forEach(link => {
      link.addEventListener('click', (e) => {
        e.preventDefault();
        const href = link.getAttribute('href');
        if (href && href.startsWith('#')) {
          const targetId = href.substring(1);
          smoothScrollTo(targetId);
          closeMenu();
        }
      });
    });

    const contactLink = document.querySelector('[href*="contact"]');
    contactLink?.addEventListener('click', (e) => {
      e.preventDefault();
      
      // Check if we're on the home page
      const currentPath = window.location.pathname;
      if (currentPath === '/' || currentPath === '/index.html') {
        // We're on home page, scroll to contact section
        smoothScrollTo('contact');
      } else {
        // We're on a different page, navigate to home with contact hash
        window.location.href = '/#contact';
      }
    });

    // Dark mode event handling
    const applyDarkMode = (isDark: boolean) => {
      // Apply any HeaderPill specific dark mode styles here if needed
    };

    // Check initial dark mode state
    const checkInitialDarkMode = () => {
      const isDark = document.documentElement.classList.contains('dark-mode');
      applyDarkMode(isDark);
    };

    // Listen for dark mode changes from HeaderDarkModeToggle
    window.addEventListener('darkModeChange', (event: Event) => {
      const customEvent = event as CustomEvent;
      applyDarkMode(customEvent.detail.isDark);
    });

    // Check initial state after a short delay to ensure toggle has loaded
    setTimeout(checkInitialDarkMode, 100);

    // Dark Mode Toggle Functionality
    const createDarkModeToggle = () => {
      const toggleContainer = document.getElementById('darkModeToggle');
      if (!toggleContainer) return;

      let isDark = localStorage.getItem('portfolio-theme') === 'dark';

      const updateUI = () => {
        if (isDark) {
          document.documentElement.classList.add('dark-mode');
          document.documentElement.classList.remove('light-mode');
        } else {
          document.documentElement.classList.add('light-mode');
          document.documentElement.classList.remove('dark-mode');
        }

        // Update button content
        toggleContainer.innerHTML = `
          <button class="dark-mode-toggle-btn" title="${isDark ? 'Switch to Light Mode' : 'Switch to Dark Mode'}" data-cursor-text="${isDark ? 'Light Mode' : 'Dark Mode'}">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              ${isDark ?
                '<circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>' :
                '<path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>'
              }
            </svg>
          </button>
        `;

        // Add click listener to new button
        const toggleBtn = toggleContainer.querySelector('.dark-mode-toggle-btn');
        toggleBtn?.addEventListener('click', toggleDarkMode);
      };

      const toggleDarkMode = () => {
        isDark = !isDark;
        localStorage.setItem('portfolio-theme', isDark ? 'dark' : 'light');
        updateUI();

        // Trigger dark mode change event for other components
        const event = new CustomEvent('darkModeChange', { detail: { isDark } });
        window.dispatchEvent(event);
      };

      // Initialize
      updateUI();
    };

    // Initialize dark mode toggle
    createDarkModeToggle();
  });
</script>