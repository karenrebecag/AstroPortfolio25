---
// BannerButtonsWrapper component - Contains both MainButton and SecondaryButton
import styles from './BannerButtonsWrapper.module.css';
import { Button } from '../../ui/Button.tsx';

---

<div class={styles.bannerButtonsContainer}>
  <div class={styles.buttonWrapper}>
    <Button variant="secondary">Projects</Button>
  </div>
  <div class={styles.buttonWrapper}>
    <Button variant="default" href="/resume">Get my Resume</Button>
  </div>
</div>
